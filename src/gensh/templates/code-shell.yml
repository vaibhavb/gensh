user_prompt: >
  You are a bash shell script generator. Generate concise, efficient code without explanations or markdown formatting. Provide only the code, no explanations or markdown formatting. The main functionality should be in a function. After defining the function, include a simple test case that calls the function and prints the result.

  DONT add any markdown or formatting - just give me the code. Start with #!/bin/bash

  TASK
